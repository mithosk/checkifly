{
  "name": "checkifly",
  "version": "0.12.2",
  "description": "Application Monitoring",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "start": "rimraf dist && tsc --build && node dist/app.js"
  }
}
