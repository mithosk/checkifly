{
  "name": "checkifly",
  "description": "Free Application Monitoring",
  "scripts": {
    "docker": "docker compose down -v && docker compose pull && docker compose up -d --build --force-recreate",
  }
}
